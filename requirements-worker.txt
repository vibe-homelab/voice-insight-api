# Worker dependencies (for macOS host)
# Updated: 2026-03 for Dia 1.6B / CSM / Whisper v3 Turbo

# Core (same as gateway)
fastapi>=0.115.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
httpx>=0.27.0
PyYAML>=6.0.2

# MLX Framework (Apple Silicon only)
mlx>=0.24.0
mlx-audio>=0.4.1
huggingface-hub>=0.28.0
soundfile>=0.13.0
numpy>=1.26.0
