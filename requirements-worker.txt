# Worker dependencies (for macOS host)
# Core (same as gateway)
fastapi>=0.115.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
httpx>=0.26.0
PyYAML>=6.0.1

# MLX Framework (Apple Silicon only)
mlx>=0.22.0
mlx-audio>=0.3.0
huggingface-hub>=0.20.0
soundfile>=0.12.1
numpy>=1.26.0
